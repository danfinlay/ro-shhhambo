(define (strategy2 history)
  (let ((history-length (length history)))
    (cond
      ((= (modulo history-length 3) 0) 'paper)
      ((= (modulo history-length 3) 1) 'rock)
      (else 'scissors))))
