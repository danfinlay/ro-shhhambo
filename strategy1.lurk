(define (strategy1 history)
  (let ((history-length (length history)))
    (cond
      ((= (modulo history-length 3) 0) 'rock)
      ((= (modulo history-length 3) 1) 'paper)
      (else 'scissors))))
